# Solution ITC: Shops Phase (Tasks 2.1 + 2.2)

```yaml
solution: "Marketplace Shops (Task 2.1 + 2.2)"
tasks_covered: [2.1, 2.2]

implementation_summary:
  entry_points:
    - "POST /api/shops — Create shop (seller-only), returns 201 with shop object (seller_id excluded)"
    - "GET /api/shops/my — Retrieve current seller's shop (seller-only, onboarding gate), returns 200 or 404"
    - "PUT /api/shops/my — Update current seller's shop (seller-only), returns 200 with updated shop"
    - "GET /api/shops/:id — Retrieve public shop detail (unauthenticated), returns 200 (seller_id excluded)"
  protected_routes:
    - "POST /api/shops — requires bearer token + seller role"
    - "GET /api/shops/my — requires bearer token + seller role"
    - "PUT /api/shops/my — requires bearer token + seller role"
  frontend_routes:
    - "GET /seller/onboarding — SellerOnboarding component (two-step: terms + shop creation)"
    - "GET /seller/shop — ShopProfile component (fetch/edit seller's shop)"
    - "GET /shops/:id — ShopDetail component (public, read-only)"

test_contract:
  tiers_required: [e2e, full_suite]
  rationale: "E2E via backend integration + frontend unit tests as proxy (Playwright E2E in Phase 8); full suite catches cross-module regressions."

  e2e:
    commands:
      - "npm run test --workspace=backend -- tests/integration/modules/shops/endpoints.integration.test.ts"
      - "npm run test --workspace=frontend -- src/api/shops.test.ts"
    scenarios:
      - "Seller creates shop with valid payload → 201 with shop object"
      - "Unauthenticated POST /api/shops → 401"
      - "Buyer attempts POST /api/shops → 403"
      - "GET /api/shops/my before shop created (onboarding gate) → 404"
      - "POST shop then GET /api/shops/my → 200 with shop (includes seller_id)"
      - "Duplicate shop creation by same seller → 409"
      - "PUT /api/shops/my updates shop → 200, partial updates preserve unmodified fields"
      - "GET /api/shops/:id public endpoint → 200 without seller_id in response"
      - "Complete onboarding flow: 404 → POST → 200 sequence"
    required_services:
      - "PostgreSQL database"

  full_suite:
    command: "npm run test -w backend && npm run test -w frontend"
    rationale: "Catch any cross-task regression not visible in individual task ITCs"

acceptance_criteria:
  e2e: all scenarios pass
  full_suite: all pass
  no_regression: true

sign_off:
  coding_agent: ✅
  testing_agent: ✅
```
