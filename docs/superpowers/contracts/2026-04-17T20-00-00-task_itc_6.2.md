# Task ITC 6.2 — Frontend Reviews UI

```yaml
task_id: 6.2
task_name: "Frontend Reviews UI"

implementation_scope:
  components:
    - "OrderDetail: Inline review form (frontend/src/pages/buyer/OrderDetail.tsx)"
    - "ProductBrowse: ReadOnly StarRating on product cards (frontend/src/pages/buyer/ProductBrowse.tsx)"
    - "ProductDetail: Fix readonly → interactive={false} prop on StarRating (frontend/src/pages/buyer/ProductDetail.tsx)"
    - "Tests: OrderDetail review form tests (frontend/src/pages/buyer/OrderDetail.test.tsx)"
    - "Tests: ProductBrowse rating tests (frontend/src/pages/buyer/ProductBrowse.test.tsx)"

  integration_surfaces:
    - "POST /reviews (via useSubmitReview hook from useReviews.ts)"
    - "GET /orders/:id (existing getOrder API)"
    - "GET /api/products (existing productsApi.getProducts)"

test_contract:
  tiers_required: [unit]
  rationale: "Frontend UI components tested in isolation with mocked API responses; no backend integration required."

  unit:
    commands:
      - "cd /Users/nguyenviethung/.config/superpowers/worktrees/marketplace-platform-superpowers/marketplace-implementation/frontend && npm test -- src/pages/buyer/OrderDetail.test.tsx"
      - "cd /Users/nguyenviethung/.config/superpowers/worktrees/marketplace-platform-superpowers/marketplace-implementation/frontend && npm test -- src/pages/buyer/ProductBrowse.test.tsx"
      - "cd /Users/nguyenviethung/.config/superpowers/worktrees/marketplace-platform-superpowers/marketplace-implementation/frontend && npm test"

    must_cover:
      - "[OrderDetail-Review] When order.status === 'completed', renders review form below each item"
      - "[OrderDetail-Review] Review form contains interactive StarRating with onChange handler"
      - "[OrderDetail-Review] Review form contains optional textarea for text input"
      - "[OrderDetail-Review] Review form contains Submit button"
      - "[OrderDetail-Review] Clicking Submit calls useSubmitReview with order_item_id, rating, and optional text"
      - "[OrderDetail-Review] On success, displays 'Review submitted' confirmation and hides form"
      - "[OrderDetail-Review] On 409 Conflict, displays 'Already reviewed' and does not show form"
      - "[OrderDetail-Review] When order.status !== 'completed', review form is not rendered"
      - "[OrderDetail-Review] Submit button is disabled until rating > 0"
      - "[OrderDetail-Review] Submit button shows loading state while mutation is pending"
      - "[OrderDetail-Review] Each item in multi-item order has independent review form and state"
      - "[OrderDetail-Review] On non-409 errors, shows error toast notification"
      - "[ProductBrowse-Rating] When review_count > 0, displays StarRating (interactive={false}, size='sm')"
      - "[ProductBrowse-Rating] StarRating displays product.aggregate_rating value"
      - "[ProductBrowse-Rating] StarRating displayed alongside review count text"
      - "[ProductBrowse-Rating] When review_count === 0, does not display StarRating"
      - "[ProductBrowse-Rating] Multiple products with different ratings display correctly"
      - "[ProductBrowse-Rating] Review count text matches product.review_count value"

acceptance_criteria:
  unit: all pass
  no_regression: true

sign_off:
  coding_agent: ✅
  testing_agent: ✅
```
