# ITC: Task 2.2 — Shops Frontend

```yaml
task_id: 2.2
task_name: "Shops Frontend (Phase 2)"

implementation_scope:
  components:
    - "SellerOnboarding page with two-step flow: accept terms + create shop (frontend/src/pages/seller/SellerOnboarding.tsx)"
    - "ShopProfile page for shop CRUD via GET /api/shops/my and PUT /api/shops/my (frontend/src/pages/seller/ShopProfile.tsx)"
    - "ShopDetail (public) page shows shop info by ID via GET /api/shops/:id (frontend/src/pages/buyer/ShopDetail.tsx)"
    - "Shops API module with CRUD endpoints (frontend/src/api/shops.ts)"
    - "App.tsx route addition for /shops/:id public shop detail page"
    - "Tests: frontend/src/pages/seller/SellerOnboarding.test.tsx"
    - "Tests: frontend/src/pages/seller/ShopProfile.test.tsx"
    - "Tests: frontend/src/pages/buyer/ShopDetail.test.tsx"
    - "Tests: frontend/src/api/shops.test.ts"

  integration_surfaces:
    - "Backend POST /api/shops: create shop (step 2 of onboarding)"
    - "Backend GET /api/shops/my: fetch current seller's shop (ShopProfile, onboarding redirect)"
    - "Backend PUT /api/shops/my: update current seller's shop (ShopProfile save)"
    - "Backend GET /api/shops/:id: fetch shop public info (ShopDetail page)"

test_contract:
  tiers_required: [unit]
  rationale: "All API calls are mocked at unit level via vi.mock(); no integration tests with live backend required."

  unit:
    commands:
      - "npm test -w frontend -- SellerOnboarding.test.tsx"
      - "npm test -w frontend -- ShopProfile.test.tsx"
      - "npm test -w frontend -- ShopDetail.test.tsx"
      - "npm test -w frontend -- src/api/shops.test.ts"

    must_cover:
      # SellerOnboarding
      - "[SellerOnboarding] Renders step 1: terms acceptance with checkbox + continue button"
      - "[SellerOnboarding] Step 1 checkbox has proper label (accessibility)"
      - "[SellerOnboarding] Continue button disabled until checkbox is checked"
      - "[SellerOnboarding] Clicking continue on step 1 advances to step 2"
      - "[SellerOnboarding] Step 2 renders form with fields: name (required), description, banner_url, contact_email"
      - "[SellerOnboarding] Step 2 form fields have proper labels (accessibility)"
      - "[SellerOnboarding] Step 2 validates name is required; shows validation error on submit if empty"
      - "[SellerOnboarding] Step 2 validates contact_email format if provided"
      - "[SellerOnboarding] Step 2 submits to POST /api/shops with form payload"
      - "[SellerOnboarding] Step 2 button disabled during submit request"
      - "[SellerOnboarding] Step 2 on successful shop creation, navigates to /seller/shop"
      - "[SellerOnboarding] Step 2 on API error, displays error message and does not navigate"
      - "[SellerOnboarding] Back button on step 2 returns to step 1"

      # ShopProfile
      - "[ShopProfile] On mount, fetches shop via GET /api/shops/my"
      - "[ShopProfile] Shows loading spinner while fetching"
      - "[ShopProfile] If shop exists, renders form with fields pre-filled with current values"
      - "[ShopProfile] Form fields: name, description, banner_url, contact_email with proper labels"
      - "[ShopProfile] Submit button calls PUT /api/shops/my with updated payload"
      - "[ShopProfile] Button disabled during save request (prevents concurrent saves)"
      - "[ShopProfile] On successful save, shows success message"
      - "[ShopProfile] On save error, shows error message"
      - "[ShopProfile] If GET /api/shops/my returns 404, redirects to /seller/onboarding"
      - "[ShopProfile] Form validation: name is required, shows error on submit if empty"
      - "[ShopProfile] Form validation: contact_email format validated if provided"

      # ShopDetail (Public Shop Page)
      - "[ShopDetail] On mount, fetches shop via GET /api/shops/:id with route param"
      - "[ShopDetail] Shows loading spinner while fetching"
      - "[ShopDetail] If shop found, displays: shop name, description, banner_url, contact_email, created_at"
      - "[ShopDetail] If shop not found (404), shows not-found message"

      # Shops API Module
      - "[shopsApi] createShop(payload) → POST /api/shops, returns Shop object"
      - "[shopsApi] getMyShop() → GET /api/shops/my, returns Shop object"
      - "[shopsApi] updateMyShop(payload) → PUT /api/shops/my, returns Shop object"
      - "[shopsApi] getShopById(id) → GET /api/shops/:id with id in URL"

acceptance_criteria:
  unit: all pass
  no_regression: true

sign_off:
  coding_agent: ✅
  testing_agent: ✅
```
