# ITC: Task 4.2 — FakeStore Integration Frontend

```yaml
task_id: 4.2
task_name: "FakeStore Integration Frontend"

implementation_scope:
  components:
    - "FakeStoreImport page component — full implementation replacing stub (frontend/src/pages/seller/FakeStoreImport.tsx)"
    - "FakeStore API module (frontend/src/api/fakestore.ts)"
    - "FakeStoreImport component test suite (frontend/src/pages/seller/FakeStoreImport.test.tsx)"
    - "FakeStore API test suite (frontend/src/api/fakestore.test.ts)"
    - "FakestoreProduct type added to shared types (frontend/src/types/index.ts)"

  integration_surfaces:
    - "Backend API: GET /api/fakestore/products → FakestoreProduct[]"
    - "Backend API: POST /api/fakestore/import → { ids: number[], overwrite?: boolean } → { importedIds: number[], count: number }"
    - "ToastContext: useToast hook for success/error notifications"
    - "React Router: useNavigate (navigate to /seller/products on import success)"

test_contract:
  tiers_required: [unit]
  rationale: "Frontend UI tests at unit level through mocked API calls. No integration tier needed — backend endpoints are covered by Task 4.1."

  unit:
    commands:
      - "npm run test --workspace=frontend -- src/pages/seller/FakeStoreImport.test.tsx src/api/fakestore.test.ts"
    must_cover:
      # Component Rendering
      - "[AC-Init] On mount, calls GET /api/fakestore/products and displays product grid with image, title, price, category"
      - "[AC-Loading] While fetching products, Spinner shown; grid hidden"
      - "[AC-Empty] Empty response (empty array) shows EmptyState"
      # Product Selection
      - "[AC-Checkbox] Each product has a checkbox; clicking toggles selection"
      - "[AC-Selection-Disabled] No selection → Import button disabled"
      - "[AC-Selection-Enabled] 1+ selection → Import button enabled"
      - "[AC-Selection-Cleared] Selection clears after successful import"
      # Import Dialog
      - "[AC-Dialog-Open] Clicking 'Import Selected' opens Modal with selected count"
      - "[AC-UI-07] Import dialog overwrite checkbox defaults to UNCHECKED (false)"
      - "[AC-Dialog-Count-Display] Modal correctly displays count of selected items"
      - "[AC-Dialog-Cancel] Cancel closes dialog without calling POST /api/fakestore/import"
      - "[AC-Dialog-Confirm] Confirm calls POST /api/fakestore/import with correct ids and overwrite"
      - "[AC-Overwrite-Checked] When overwrite checkbox is checked, POST includes overwrite: true"
      - "[AC-Overwrite-Unchecked] When overwrite checkbox is unchecked, POST includes overwrite: false"
      - "[AC-Overwrite-State-Persistence] Overwrite checkbox state persists when reopening dialog"
      # Import Submission
      - "[AC-Loading-Import] While importing, Modal confirm button shows loading state and is disabled"
      - "[AC-Import-Success] On import success, toast shown + navigate to /seller/products"
      # Error Handling — GET failures (AC-03)
      - "[AC-03-GET-Error] When GET /api/fakestore/products fails, error banner displayed with descriptive message"
      - "[AC-03-Retry] Clicking Retry button re-calls GET /api/fakestore/products"
      - "[AC-03-Retry-Success] After Retry succeeds, products grid displays; error banner hidden"
      - "[AC-03-Retry-Persistence] Multiple retries work (error → retry → error → retry → success)"
      # Error Handling — POST failures (AC-07)
      - "[AC-07-POST-Error] When POST /api/fakestore/import fails, error banner displayed"
      # Edge-case Rendering — malformed backend responses (AC-07)
      - "[AC-07-Malformed-Title] Products with title='Untitled Product' render correctly"
      - "[AC-07-Malformed-Price] Products with price=0.01 render correctly"
      - "[AC-07-Malformed-Category] Products with category='uncategorized' render correctly"
      - "[AC-07-Malformed-Image] Products with null image_url show fallback/placeholder"
      - "[AC-07-Malformed-Rating] Products with aggregate_rating=0 render correctly"
      # API Client tests (fakestore.test.ts)
      - "[API-getProducts] getProducts() calls GET /api/fakestore/products"
      - "[API-getProducts-Response] getProducts() returns array of FakestoreProduct objects"
      - "[API-getProducts-Error] getProducts() propagates API errors"
      - "[API-importProducts] importProducts(ids, overwrite) calls POST /api/fakestore/import"
      - "[API-importProducts-Payload] POST payload includes { ids: number[], overwrite: boolean }"
      - "[API-importProducts-Response] importProducts() returns { importedIds: number[], count: number }"
      - "[API-importProducts-Error] importProducts() propagates API errors"

acceptance_criteria:
  unit: all pass
  no_regression: true

sign_off:
  coding_agent: ✅
  testing_agent: ✅
```
